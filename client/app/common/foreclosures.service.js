const foreclosuresServiceFactory = function () {
  // TODO: implement methods:
  // setAll(realEstates)
  // toggleSelected(realEstate)
  // isSelected(realEstate)

  return {
    realEstates: null,
    selectedRealEstate: null
  };
};

export default foreclosuresServiceFactory;